# Products Desktop App (JavaFX + SQLite)

Standalone Maven project for Products component.

## Run

```bash
mvn clean javafx:run
```

## Eclipse

Import as **Existing Maven Project** and run `MainApp` or `javafx:run`.

Database is auto-created on first run at:

`~/.marketplace-products/products.db`
